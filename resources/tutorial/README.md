# Guided-tour fixtures

These optimized images are bundled so the guided tour never performs network
I/O. Attribution and modification details live in `TUTORIAL_ASSETS.json` and
must remain beside the files in every packaged build.
