{
  "name": "picto-node",
  "version": "0.5.0-alpha",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4"
  }
}
