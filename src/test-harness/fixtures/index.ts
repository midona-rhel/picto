/**
 * Fixture index — import fixtures for any rebuilt slice from here.
 *
 * Usage in a rebuilt component test or lab:
 *   import { sidebar, grid, inspector } from '../../test-harness/fixtures';
 */

export * as sidebar from './sidebar';
export * as grid from './grid';
export * as inspector from './inspector';
